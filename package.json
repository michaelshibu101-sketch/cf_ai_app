{
  "name": "cf-ai-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "agents": "^0.5.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.9.1"
  }
}
